{
  "name": "filemanager_middleware",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start server.js --name filemanager_middleware",
    "stop": "pm2 stop filemanager_middleware",
    "restart": "pm2 restart filemanager_middleware",
    "status": "pm2 status"
  },
  "private": true,
  "dependencies": {
    "express": "^4.21.2",
    "pkill": "^2.0.0"
  }
}
